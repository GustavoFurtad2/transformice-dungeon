function genMap()

    local x = 400
    local y = 31929

    local width = 400
    local height = 80
    local angle = 0

    local lastWidth, lastHeight, lastHeight

    for i = 1,10 do

        local lastWidth, lastHeight, lastHeight = width, height, angle

        width = math.random(50, 100)
        angle = math.random(0, 360)

        
    end
end
